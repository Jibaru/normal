{
  "name": "@whatsapp-mcp/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./api-key-hmac": "./src/api-key-hmac.ts",
    "./connection-setup": "./src/connection-setup.ts",
    "./deployment": "./src/deployment.ts",
    "./whatsapp-connection": "./src/whatsapp-connection.ts"
  },
  "scripts": {
    "build": "bun build src/*.ts --outdir dist --target bun",
    "lint": "biome check .",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5"
  }
}
