{
  "name": "@whatsapp-mcp/recovery-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "CI=true wrangler deploy --dry-run --env production --outdir dist",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@whatsapp-mcp/api": "workspace:*",
    "@whatsapp-mcp/contracts": "workspace:*",
    "@whatsapp-mcp/db": "workspace:*",
    "@whatsapp-mcp/neon-recovery": "workspace:*",
    "@whatsapp-mcp/restore-coordinator": "workspace:*",
    "effect": "3.22.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.19.1",
    "typescript": "^5",
    "vitest": "4.1.10",
    "wrangler": "4.116.0"
  }
}
